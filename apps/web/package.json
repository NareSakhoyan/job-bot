{
  "name": "@job-bot/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "next dev --port 3000",
    "build": "next build",
    "start": "next start --port ${PORT:-3000}",
    "typecheck": "tsc -p tsconfig.json",
    "build:isolated": "NEXT_DIST_DIR=.next-build next build",
    "start:isolated": "NEXT_DIST_DIR=.next-build next start --port ${PORT:-3000}"
  },
  "dependencies": {
    "@clerk/nextjs": "^7.7.9",
    "@job-bot/database": "workspace:*",
    "@job-bot/matching": "workspace:*",
    "@job-bot/shared": "workspace:*",
    "next": "^15.1.3",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.0.0",
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.7.2"
  }
}
